# Alchemy API key for multi-chain RPC access (recommended)
ALCHEMY_API_KEY=

# Override RPC for specific chains (format: RPC_CHAINNAME=url)
# Falls back to Alchemy if key is set, then to public RPCs
# RPC_CELO=https://...
